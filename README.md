# sys_os
1)
    sorter - производит сортировку файлов по директориям;
    sorter_back - откат сортировки;
    for_sorter - директория для работы sorter;
2)
    log_analyzer.py - поиск слова в логах построчно через полиномиальное хеширование;
    sample.log - файл с примерами логов для тестирования;
